on:
  push:
    branches: [ "test_action_required" ]

name: Test Workflow
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Running build step"

  approve:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: manual-approval  # 👈 this triggers manual review
    steps:
      - run: echo "This runs after manual approval"
