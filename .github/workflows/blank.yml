name: Manual Approval Example

on:
  workflow_dispatch: # Trigger workflow manually from GitHub UI

jobs:
  approval_job:
    name: Job Requiring Manual Approval
    runs-on: ubuntu-latest
    environment: staginig # Associate the job with the environment
    steps:
      - name: Approval Needed
        run: echo "Waiting for manual approval to continue"
