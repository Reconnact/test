on:
  push:
    branches: [ "test_dependency" ]

name: Test Workflow
jobs:
  job1:
    runs-on: flow-runner-UvFEA7msrdXN
    steps:
      - name: Step that always fails
        run: |
          echo "This job will now fail."

  job2:
    needs: job1
    runs-on: flow-runner-UvFEA7msrdXN
    if: false
    steps:
      - name: Wait for 10 seconds
        run: |
          echo "Waiting 10 seconds..."
          sleep 10
