on:
  push:
    branches: [ "test_dependency" ]

name: Test Workflow
jobs:
  job1:
    runs-on: flow-runner-UvFEA7msrdXN
    env:
      SHOULD_NOT_EXIST: ${{ secrets.NON_EXISTENT_SECRET }}  # This secret is not defined
    steps:
      - name: Explode early
        run: echo "this will never run"

  job2:
    needs: job1
    runs-on: flow-runner-UvFEA7msrdXN
    steps:
      - name: Wait for 10 seconds
        run: |
          echo "Waiting 10 seconds..."
          sleep 10
