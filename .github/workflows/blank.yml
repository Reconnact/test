on:
  push:
    branches: [ "main" ]

name: Test Workflow
jobs:
  example1:
    runs-on: [flow-runner-Kb2lEX8KewyV]
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4

  example2:
    runs-on: [flow-runner-Kb2lEX8KewyV]
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4

  example3_depends:
    runs-on: [flow-runner-Kb2lEX8KewyV]
    needs: [example1, example2]
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4
      - run: echo "😴 Time for a nap, sleeping for 20s" && sleep 20
