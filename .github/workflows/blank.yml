on:
  push:
    branches: [ "test_dependency" ]

name: Test Workflow
jobs:
  job1:
    runs-on: flow-runner-UvFEA7msrdXN
    steps:
      - name: Explode early
        if: ${{ contains(needs.nonexistent_job.result, 'success') }}
        run: echo "this will never run"

  job2:
    needs: job1
    runs-on: flow-runner-UvFEA7msrdXN
    steps:
      - name: Wait for 10 seconds
        run: |
          echo "Waiting 10 seconds..."
          sleep 10
