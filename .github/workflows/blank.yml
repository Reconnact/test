on:
  push:
    branches: [ "main" ]

name: Test Workflow
jobs:
  example1:
    runs-on: [flow-runner-xiItjx7mszVa]
    steps:
      - run: echo "Done"
      - run: sleep 10
  example2:
    runs-on: [flow-runner-xiItjx7mszVa]
    steps:
      - run: sleep 30
      - run: echo "Done"
