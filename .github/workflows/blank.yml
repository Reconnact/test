on:
  push:
    branches: [ "test_action_required" ]

name: Test Workflow
jobs:
  neutral_job:
    runs-on: ubuntu-latest
    steps:
      - name: Exit with code 78
        run: exit 78
  approve:
    needs: neutral_job
    runs-on: flow-runner-PGOTeiRw1P4h
    environment:
      name: staging  # 👈 this triggers manual review
    steps:
      - run: echo "This runs after manual approval"
