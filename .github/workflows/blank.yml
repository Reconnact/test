on:
  push:
    branches: [ "test_failing_job" ]

name: Test Workflow
jobs:
  job1:
    runs-on: flow-runner-UvFEA7msrdXN
    steps:
      - name: Step that always fails
        run: |
          echo "This job will now fail."
          exit 1  # Explicit failure
